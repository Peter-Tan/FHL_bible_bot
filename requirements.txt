# Server runtime dependencies (Python 3.9+).
# The production venv (.venv/) already has these; for a fresh machine:
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
anthropic>=0.97.0
fastapi>=0.128.0
uvicorn>=0.39.0
requests>=2.32.0
python-dotenv>=1.2.0
openai>=1.40.0  # v8 engine: local vLLM OpenAI-compatible endpoint
